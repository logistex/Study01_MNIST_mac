<!-- 2026-08-06 19:28 KST 생성 -->

# CLAUDE.md — 데스크톱 버전

MNIST 손글씨 숫자 인식의 맥용 데스크톱 버전이다. CNN을 학습하고, Tkinter 그림판에 마우스로 그린 숫자를 인식한다.

학습과 가중치 관리는 이 폴더가 맡는다. 웹 버전(`../web_version/`)은 여기서 내보낸 가중치를 받아 쓴다.

## 명령

```bash
cd desktop_version        # train.py 가 상대 경로를 쓰므로 이 폴더 안에서 실행한다
python3 train.py          # 학습 후 mnist_cnn.pt 저장 (5회차, MPS 사용, 수 분 소요)
python3 app.py            # 인식 앱 실행
python3 가중치내보내기.py     # 웹 버전이 쓸 가중치.bin, 가중치정보.json 생성
python3 검증데이터만들기.py   # 웹 버전 검증용 정답 데이터 생성
open 손글씨인식.app         # 앱 번들로 실행 (독에 넣어 쓰는 방식)
```

`train.py`는 데이터 경로를 `root="data"`라는 상대 경로로 쓴다. 그래서 프로젝트 루트에서 `python3 desktop_version/train.py`로 실행하면 데이터를 찾지 못한다. **반드시 이 폴더로 들어가서 실행한다.** 새로 만든 스크립트 둘은 `Path(__file__)` 기준 절대 경로를 쓰므로 어디서 실행해도 된다.

테스트 프레임워크는 없다. 검증이 필요하면 `preprocess.mnist형식으로변환` + 모델 예측 경로를 직접 호출하는 임시 스크립트를 쓴다. MNIST 테스트 이미지를 280x280으로 키워 앱과 같은 경로로 통과시키면 98% 내외가 나와야 정상이다.

## 환경 제약 (중요)

이 맥의 제약 때문에 일반적인 구성과 다르다. "고치려" 들기 전에 확인할 것.

- **파이썬 3.14뿐이고 TensorFlow는 3.14용 배포판이 없다.** 그래서 Keras가 아니라 PyTorch(2.13)로 구현했다. Keras로 바꾸려면 파이썬 3.12나 3.13을 따로 설치해야 한다.
- **onnx 패키지도 3.14용 휠이 없다.** free-threaded 빌드(`cp314t`)용만 있는데 이 맥의 파이썬은 GIL이 켜진 일반 빌드다. 그래서 웹 버전은 ONNX 대신 가중치를 직접 내보내는 방식을 쓴다.
- **MNIST 원본은 저장소에 없다.** 63MB라 `.gitignore`로 제외했다. 저장소를 새로 받았다면 아래 명령으로 먼저 데이터를 채워야 `train.py`와 `검증데이터만들기.py`가 돈다.

  ```bash
  mkdir -p desktop_version/data/MNIST/raw
  cd desktop_version/data/MNIST/raw
  for name in train-images-idx3-ubyte train-labels-idx1-ubyte t10k-images-idx3-ubyte t10k-labels-idx1-ubyte; do
    curl -O "https://ossci-datasets.s3.amazonaws.com/mnist/$name.gz"
  done
  ```

  `.gz` 4개만 받으면 된다. 압축을 푸는 것은 torchvision 이 알아서 한다.

- **MNIST 자동 내려받기는 SSL 인증서 오류로 실패한다.** (`CERTIFICATE_VERIFY_FAILED`) 그래서 위처럼 `curl`로 직접 받는다. `download=True`는 파일이 이미 있으면 건너뛰므로, 받아 두기만 하면 코드를 고칠 필요가 없다. 근본 해결은 `/Applications/Python 3.14/Install Certificates.command` 실행(관리자 권한 필요).
- 학습은 애플 실리콘 GPU(MPS)를 쓴다. 예측은 CPU로 한다.

## 구조

네 파일이 하나의 사슬로 엮여 있다. 한 곳을 바꾸면 다른 곳도 같이 손봐야 한다.

- **`model.py`** — CNN 구조. `train.py`와 `app.py`가 **함께 임포트한다.** 구조를 바꾸면 `mnist_cnn.pt`의 `state_dict`와 어긋나므로 **반드시 재학습해야 한다.** 웹 버전의 `모델.js`도 이 구조를 손으로 옮긴 것이라 함께 고쳐야 한다.
- **`train.py`** — 학습 전용. 데이터 적재, 학습 루프, 평가, 가중치 저장.
- **`preprocess.py`** — 캔버스 그림을 MNIST 형식으로 바꾸는 전처리. **이 프로젝트에서 가장 깨지기 쉬운 부분이다.**
- **`app.py`** — Tkinter GUI. 그리기, 전처리 호출, 예측, 결과 표시.

웹 버전을 위한 스크립트 둘이 더 있다.

- **`가중치내보내기.py`** — `mnist_cnn.pt`를 `../web_version/가중치.bin`과 `가중치정보.json`으로 내보낸다. 학습을 다시 했다면 이것도 다시 돌려야 웹 버전이 새 가중치를 쓴다.
- **`검증데이터만들기.py`** — MNIST 테스트 이미지 200장을 280x280으로 키운 그림과, 파이썬 전처리 결과, 예측 확률을 `../web_version/검증데이터.json`에 담는다. 이 파일은 git에 넣지 않는다.

#### 검증 표본에 사각지대가 있다

`검증데이터만들기.py`가 만드는 200장은 **전부 MNIST 28x28을 최근접 이웃으로 10배 확대한 것**이다. 그래서 여백을 잘라낸 바운딩 박스의 변 길이가 **항상 10의 배수**가 된다. 실측으로 확인한 결과다.

```
바운딩박스 변 길이 mod 10 = {(0, 0)}      ← 200장 전부
비율>1(확대) 경로 진입 표본 수 = 0/200
가로비, 세로비가 정수인 표본 = 200/200
```

세 번째 줄이 문제다. 축소 비율이 정수라 `면적평균축소`의 **부분 겹침 산술이 한 번도 실행되지 않는다.** 함수가 단순 블록 평균으로 퇴화하고, 부동소수 인덱스 초과 경계도 절대 안 걸린다. 실제로 이 사각지대 때문에 출력 784개가 전부 `NaN`이 되어 무엇을 그리든 "0"이라고 답하던 결함을 **검증이 통과시켰다.** 사람이 코드를 읽어 찾아냈다.

**보완하려면** `app.py`의 그리기 알고리즘(선 + 이음매 원, 붓굵기 18)으로 PIL에 획을 직접 그린 표본 20~30장을 섞는다. 다만 합성 획에는 정답 라벨이 없으므로 **정확도 지표에는 넣지 말고 순전파, 전처리 일치 검사에만 쓴다.** 97% 정확도 기준선은 기존 MNIST 200장으로 유지한다.

**아직 하지 않았다.** 지금 알려진 결함은 없고 실패 모드도 즉시 드러나는 종류라 급하지 않다.

### 전처리를 건드릴 때

캔버스 그림을 단순히 28x28로 축소하면 인식률이 무너진다. MNIST는 다음 규칙으로 만들어진 데이터이고, `mnist형식으로변환`이 그 규칙을 그대로 재현한다.

1. 여백을 잘라내고 → 2. 비율 유지하며 20x20에 맞추고 → 3. **밝기 무게중심을 28x28 한가운데로** 옮긴다.

세 단계 중 하나라도 빠지면 정확도가 크게 떨어진다. 바꿨다면 위에 적은 98% 검증을 반드시 다시 돌린다. **웹 버전의 `전처리.js`도 같은 세 단계를 구현하고 있으므로 함께 고치고, `web_version/검증.html`로 다시 확인한다.**

### 값이 중복된 곳

정규화 상수 `0.1307`, `0.3081`이 **두 곳에 따로 적혀 있다.** 학습과 예측이 어긋나면 안 되므로 한쪽만 고치지 않는다.

- `train.py`의 `transforms.Normalize`
- `preprocess.py`의 `평균`, `표준편차`

웹 버전은 이 값을 직접 갖지 않는다. `가중치내보내기.py`가 `preprocess.py`에서 읽어 `가중치정보.json`에 실어 보낸다.

### GUI에서 알아 둘 점

- 캔버스에 그리는 동시에 **같은 내용을 PIL 이미지에 따라 그린다.** 화면 화소를 직접 읽는 방식은 운영체제마다 달라 신뢰할 수 없기 때문이다. `그리기시작`, `그리는중`은 항상 두 곳에 같이 그려야 한다.
- PIL의 `line`은 선 끝을 둥글게 처리하지 않으므로 이음매마다 원을 덧그린다. 이 보정을 빼면 획이 각지게 남는다.
- `붓굵기 = 18`은 280px 캔버스를 28px로 줄였을 때 MNIST 획 굵기와 비슷해지도록 맞춘 값이다.
- `모델파일`은 `Path(__file__).parent` 기준으로 찾는다. 파인더 더블클릭이나 앱 번들 실행에서는 현재 폴더가 달라지기 때문이다.

## 맥 실행 방식 두 가지

**`손글씨인식.app` 번들** — `Contents/MacOS/Python`은 프레임워크 파이썬 실행 파일의 **복사본**이다. 번들 안에서 실행되어야 독이 "Python"이 아니라 이 앱의 이름과 아이콘을 보여 준다. `launcher` 스크립트가 자기 위치에서 세 단계 위를 프로젝트 폴더로 삼으므로, **`.app`은 `app.py`와 같은 폴더에 있어야 한다.** 즉 이 폴더 안에 있어야 한다. 번들 내용을 수정했다면 다시 서명한다.

```bash
codesign --force --deep --sign - 손글씨인식.app
```

**`app.py` 더블클릭** — `com.apple.LaunchServices.OpenWith` 확장 속성으로 Python Launcher에 연결해 두었다. 파일을 복사하면 이 속성은 따라가지 않을 수 있다. 앱 번들 방식은 Python Launcher 환경설정과 무관하다.

## 작성 규칙

- 코드의 **식별자와 주석을 모두 한글로 쓴다.** 함수, 변수 이름도 한글이다 (`손글씨분류모델`, `그리는중`, `붓굵기`).
- 예외는 셸 스크립트다. 셸은 변수 이름에 한글을 쓸 수 없어 `launcher`에서만 영문 변수를 쓴다. 주석은 한글로 유지한다.
